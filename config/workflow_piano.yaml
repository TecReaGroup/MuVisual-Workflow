enabled: true
overwrite: false
instrument: piano

workflow:
  - step: separation
    option: bs_roformer_sw
  - step: audio_to_midi
    option: muscriptor_piano
  - step: music_metadata
    option: key_bpm_delay
  - step: midi_quantization
    option: default
